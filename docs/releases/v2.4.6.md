# AltForge v2.4.6

- Status: Published; superseded by v2.4.7, real-device acceptance pending
- Published: 2026-09-05 14:55:36 Asia/Shanghai
- Tag commit: `879d50774078d7ec85d6df14a1a181710ed5b050`
- Build number: 24
- Workflow: https://github.com/legeling/AltForge/actions/runs/33949821997
- Version: 2.4.6 for iOS AltForge, macOS AltForge Server and Windows AltForge Server
- Change: CHG-20260905-003; device acceptance remains tracked by ISSUE-20260905-003
- Release: https://github.com/legeling/AltForge/releases/tag/v2.4.6
- AltSign: unchanged from v2.4.5 (`9b06df71234804204bbbcb65ed135edd0d15b8cb`)

## 更新内容

- 修复后台安装完成后可能遗漏管理记录的问题：安装前保存恢复信息，收到成功回执立即保存记录；回到前台后有限次数核对系统登记，确认已安装后补回“我的 App”。
- 不再因暂时查不到系统登记就自动删除应用记录与刷新缓存；恢复过程避开仍在运行的安装任务，并避免重复记录。
- IPA 导入显示安装阶段、进度和耗时，保留完成或错误结果。安装阶段断线、超时或取消时显示“安装结果待确认”，不直接断言设备安装失败。
- 手动安装和刷新期间防止自动锁屏，结束后恢复原设置；这不绕过手动锁屏或 iOS 后台运行限制。
- 统一安装、认证、设置及通用控件的主题色，改善大字体和导航安全区域布局；错误和到期提醒仍保留语义警示色。
- iOS App 与 macOS、Windows 服务端同步版本号；桌面认证逻辑沿用 v2.4.5，本次没有新的 Windows 登录修复。

## 验证与边界

- 本地 28 项不同 XCTest 已通过分批验证，iOS 模拟器构建通过。覆盖安装记录恢复、延迟登记、前后台切换、重试上限、并发状态读取、去重、保存失败回滚、进度、常亮与主题布局。
- 用户要求先发布再做真机测试。真实后台安装、恢复记录后的再次刷新及完整设备视觉矩阵尚未验收，不能将模拟器结果描述为真机问题已彻底解决。
- 标签 CI 的全部 28 项 XCTest、iOS Release、Universal macOS Release、Windows 构建和打包验证均通过。
- 本机下载 Release 大文件速度异常慢，已停止这两次有界尝试，改从同一次 CI 的 artifact 下载三个安装包；配置与校验和仍从 Draft 下载。九个文件的大小及 SHA-256 全部与 GitHub Draft asset metadata 一致，校验和清单的八项也全部通过。没有手工重打包或替换资产。
- 本机下载包验证通过：IPA/DMG 身份、2.4.6 版本、build 24、DMG Applications 链接、Universal 架构、deep ad-hoc 签名及未 Developer ID 签名策略；Windows FILEVERSION/PRODUCTVERSION 均为 2.4.6.0。apps.json 使用版本固定 IPA URL，校验大小和摘要匹配，并保留七个版本。
- 公开 latest API 已确认非 Draft、非预发布的 v2.4.6；公开下载的 apps.json 与发布前校验内容一致。安装包公开链接已检查。
- 三端安装包的匿名 latest 下载链接均返回 HTTP 200。本轮 CI 观察、下载和验证进程均已退出；临时下载、未完成下载、验证挂载、运行日志缓存均已清理，未修改系统代理或用户运行中的服务端。
- IPA 未签名，需要服务端签名安装。macOS 使用 ad-hoc 完整性签名，未 Developer ID 签名或公证；Windows 二进制未签名。

## 升级与回滚

发布后确认的已知问题：官方 apps.json 漏声明 NSLocalNetworkUsageDescription，会导致手机端应用内更新触发 VerificationError 201。此次发布前的测试及 checksum 门禁未覆盖这个权限契约。修复由 ISSUE-20260905-004 / CHG-20260905-004 跟踪；本记录不表示公开资产已经更换。不要通过卸载或关闭安全校验处理此错误。

该声明修复已随 v2.4.7 发布。请刷新官方软件源后更新；v2.4.6 固定版本资产保持不变。

先更新电脑上的 AltForge Server，再覆盖更新手机上的 AltForge，保留现有应用和数据。macOS 更新器下载并校验 DMG 后打开安装窗口，不会静默替换正在运行的程序。

手机仍使用旧版且应用内更新无法通过认证时，请从新版电脑服务端覆盖安装 AltForge，不要先卸载手机端。

旧版本已经遗漏的应用若没有留下恢复信息，无法仅凭 App ID 自动重建。保留手机上的应用，使用原 Apple ID/team 重新导入原 IPA，不要先卸载。

没有 Core Data schema 迁移。v2.4.5 安装包仍可用于回滚，但旧版不识别新增恢复记录且仍含旧的清理逻辑，可能再次丢失管理记录，不建议为排查本问题回退。

## SHA-256

```text
e6df338c393be50af35301cf12e9e10a91963e720eb9a8d5f16a1dd279000293  AltForge.ipa
f24eab19e0cc508efdd92c19ed04680bd7d500587a6a628f357a1eb2d5871d7a  AltForge-AltServer-macOS.dmg
f1c463030168d74d3297dbfceb3996567a0927e5fb92810bce007362b0d385cc  AltForge-AltServer-Windows.zip
9b5ef52e35c6b69ba294b96e26250a09e2ae7a9fa6bfd22a5c61aba6462e9aa7  apps.json
3eed20cbc486640593e364dadab345254f6c0d2980b7c27d5a9c1c11547b028d  SHA256SUMS.txt
```
